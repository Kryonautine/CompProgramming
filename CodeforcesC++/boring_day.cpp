// Codeforces Round 942 (Div. 2) :: Problem B

#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin >> t;

    for (int i = 0 ; i < t ; i++) {
        long long n, l, r;
        cin >> n >> l >> r;
        long long a[n];
        for (long long j = 0 ; j < n ; j++) {
            cin >> a[j];
        }
    }
}